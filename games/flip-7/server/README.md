# Flip 7 Server

This folder is reserved for the Flip 7 server module.

The F7 server should stay independent from the shared GameHub pages and from other games. Future work can add the game API, room/session state, deck and discard state, validation for flips and stopping, scoring rules, and container settings here.
