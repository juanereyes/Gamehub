# Liar's Dice Server

This folder is reserved for the Liar's Dice server module.

The LD server should stay independent from the shared GameHub pages and from other games. Future work can add the game API, room/session state, validation for bids and challenges, and container settings here.
